# Secure fence examples

Regular paragraph before.

```secure
API_KEY=sk-live-abc123
```

A longer secret still uses the **same fixed-length mask** when hidden (length of the real value is not shown):

```secure
Line one
Line two (literal text — not Markdown)

**this is shown as characters, not bold**
- list syntax is literal too
```

After the block.

---

Styling matches **`pre` / `code`** like normal fenced blocks; see [`README.md`](../README.md#code-block-integration).
